import { BookingFlow } from '../../components/BookingFlow';

export const metadata = { title: 'Book Your Stay', robots: { index: false, follow: false } };

export default function Booking() { return <BookingFlow />; }
