import {Controller,Get,Query,UseGuards} from '@nestjs/common';import {PrismaService} from '../../common/prisma.service';import {JwtAuthGuard} from '../../common/jwt-auth.guard';import {RolesGuard} from '../../common/roles.guard';import {Roles} from '../../common/roles.decorator';
@Controller('audit-logs')@UseGuards(JwtAuthGuard,RolesGuard)@Roles('SUPER_ADMIN','ADMIN','VIEWER')export class AuditController{constructor(private p:PrismaService){}@Get()list(@Query('entityType')entityType?:string){return this.p.auditLog.findMany({where:{entityType:entityType||undefined},orderBy:{createdAt:'desc'},take:200,include:{actor:{select:{name:true,email:true}}}})}}
